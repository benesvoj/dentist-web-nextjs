import {PageBody} from "@/app/ui/PageBody";
import {urls} from "@/app/lib/urls";

export default function Recommendation() {
    return(
        <PageBody title={urls[3].title}>

        </PageBody>
    )
}